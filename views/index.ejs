<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
</head>

<body>
    <h1>Games</h1><button>Add Game</button>

    <div class="games-container">
        <% games.forEach((game, index)=> { %>
            <article class="game-card">
                <h2>
                    <%= game.title %>
                </h2>
                <p>Genres:</p>
                <p>Developers:</p>
            </article>
            <% }) %>

    </div>
</body>

</html>